#include<stdio.h>

void inp(char a[])

{
	printf("enter name : ");
	scanf("%[^\n]",&a);
}

void length(char a[])
{
	int i,count=0;
	
	for(i=0;a[20]!=NULL;i++)
	{
		count++;
	}
	printf("name length %d : ",str);
}

void main()
{
	inp(a[]);
	length(a[]);
}

