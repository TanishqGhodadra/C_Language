#include<stdio.h>

void inp(char a[])

{
	printf("enter name : ");
	scanf("%[^\n]",&a);
}

void length(char a[])
{
	int i,count=0;
	
	for(i=0;i!=NULL;i++)
	{
		count++;
	}
	printf("name  has %d length: ",count);
}

void main()
{

    void inp (char a[20]);
	void length (char a[20]);
}
